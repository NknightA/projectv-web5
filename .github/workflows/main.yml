name: projectv

on:
  push

jobs:
  test-run:
    runs-on: ubuntu-latest
    steps:
      - name: install packages
        run: npm ci
      - name: build pages
        run: npm run pre-build
      - name: run pages
        run: npm start
