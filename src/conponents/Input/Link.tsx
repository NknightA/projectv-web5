export const Input = ({linkhref, link}: any) => {
    return (
        <a href={linkhref}
            style={{
                textDecoration: 'none',
                color: '#FFFFFF',
            }}>
            {link}
        </a>
    )
}

export const Copy = ({linkhref, link}: any) => {

}

